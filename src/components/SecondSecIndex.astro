---
import SectionCardIndex from "../assets/SectionCardIndex.astro";
import Card from "./curriculum/SummaryCard.astro";
import { experiences } from "../data/Experience";
import Cv from "../icons/Cv.astro";
const exp = experiences.slice(0, 2);

import HeaderArticle from "./HeaderArticle.astro";
import MainArticle from "./MainArticle.astro";
---

<SectionCardIndex>
  <!-- section of curriculum -->

  <!-- header -->
  <HeaderArticle
    title="curriculum"
    nameArea="[grid-area:header]"
    slot={"content-into-card"}
  >
    <Cv slot={"icon-title"} />
    Mi formación académica está enfoca en el desarrollo web, con énfasis en frontend
    y aprendizaje de backend, complementada con proyectos prácticos.
  </HeaderArticle>

  <!-- main -->
  <MainArticle
    hredBtn="/Curriculum/"
    className="[grid-area:main] hidden md:block w-11/12 mx-auto"
    slot={"content-into-card"}
  >
    {
      exp.map(({ position, companyName, date, summary }) => (
        <Card
          position={position}
          companyName={companyName}
          dates={date}
          summary={summary}
        />
      ))
    }
  </MainArticle>

  <!-- footer -->

  <!-- out card-mobile -->
  <MainArticle hredBtn="/Curriculum/" slot={"content-out-card"}>
    {
      exp.map(({ position, companyName, date, summary }) => (
        <Card
          position={position}
          companyName={companyName}
          dates={date}
          summary={summary}
          inGreen
        />
      ))
    }
  </MainArticle>
</SectionCardIndex>
