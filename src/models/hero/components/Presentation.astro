---
interface Props {
  isDark?: boolean;
  className?: string;
}
const { isDark = true, className } = Astro.props;
const textColor: string = isDark
  ? "dark:text-whiteHaze"
  : "text-whiteHaze dark:text-woodSmoke";
---

<div class:list={[textColor, className, "flex flex-col gap-4"]}>
  <h1 class="text-5xl font-bold">¡Hola! 👋</h1>
</div>
