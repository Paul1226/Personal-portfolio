---
import TitlePage from "../../assets/TitlePage.astro";
import SectionCardIndex from "../../assets/SectionCardIndex.astro";
import ButtonsPage from "../../assets/buttons/ButtonsPage.astro";
import Card from "../curriculum/SummaryCard.astro";
import HeaderArticle from "../HeaderArticle.astro";
import MainArticle from "../MainArticle.astro";
import { projects } from "../../data/Projects";
import Briefcase from "../../icons/Briefcase.astro";
const exp = projects.slice(0, 2);
---

<SectionCardIndex>
  <!-- section of curriculum -->

  <!-- header -->
  <HeaderArticle
    title="proyectos"
    nameArea="[grid-area:header]"
    slot={"content-into-card"}
  >
    <Briefcase slot={"icon-title"} />
    Desarrollador web especializado en frontend. Actualmente, estoy ampliando mis
    habilidades al explorar e implementar conceptos de backend para desarrollar aplicaciones
    más completas.
  </HeaderArticle>

  <!-- main -->
  <MainArticle
    hredBtn="/Projects/"
    className="[grid-area:main] hidden md:block w-11/12 mx-auto"
    slot={"content-into-card"}
  >
    {exp.map(({ summary, name }) => <Card position={name} summary={summary} />)}
  </MainArticle>

  <!-- footer -->

  <!-- out card-mobile -->
  <MainArticle hredBtn="/Projects/" slot={"content-out-card"}>
    {
      exp.map(({ summary, name }) => (
        <Card position={name} summary={summary} inGreen />
      ))
    }
  </MainArticle>
</SectionCardIndex>
